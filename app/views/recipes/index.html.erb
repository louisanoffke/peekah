<h1> My Recipes </h1>

<ul>
  <% @recipes.each do |recipe| %>


  <li><%=link_to "#{recipe.name}", recipe_path(recipe) %>
    <br>
    <br>
    <br>
    <br>
    <p> Assign This Recipe To <%= current_user.restaurant.name %> & Add a price (need to make this form appear after clicking a button --- class active etc) </p>
    <!-- IF the recipe is not a dish then display the button for a form to convert the recipe to a dish.
    If it is a dish have the dish details appear instead of the button.
    i.e. the price displayed -->
    <%= render partial: "dishes/form", locals: { recipe: recipe, dish: @dish ? @dish : Dish.new } %>
  </li>
  <br>


  <%= link_to recipe_path(recipe), method: :delete, data: { confirm: "Sure about that?"} do %><i class="far fa-trash-alt recipe-icon"></i><% end %>
  <%= link_to edit_recipe_path(recipe) do %><i class="far fa-edit recipe-icon"></i><% end %>
  <% end %>
</ul>

<%= link_to "Add A Recipe", new_recipe_path, class: "btn-pka-2"  %>
<br>
<br>
<%= link_to "Home", root_path, class: "btn-pka-2" %>
